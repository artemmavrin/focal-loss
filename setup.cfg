[aliases]
test=pytest

[tool:pytest]
addopts=--verbose --doctest-modules
filterwarnings=
    ignore::DeprecationWarning
    ignore::FutureWarning

[coverage:run]
branch = True
source = focal_loss

[coverage:report]
show_missing = True
