dist: xenial
language: python
python:
  - "3.6"
  - "3.7"
install:
  - pip install --upgrade tensorflow>=2.0.0
  - make dev
  - pip install codecov
script:
  - make test
  - codecov
notifications:
  email:
    on_success: never
    on_failure: always
