dist: xenial
language: python
python:
  - "3.7"
install:
  - pip install tensorflow==2.0.0-rc2
  - make dev
  - pip install codecov
script:
  - make test
  - codecov
notifications:
  email:
    on_success: never
    on_failure: always
